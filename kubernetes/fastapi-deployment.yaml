apiVersion: apps/v1
kind: Deployment
metadata:
    name: fastapi-deployment
    namespace: minecraft-servers
spec:
    replicas: 1
    selector:
        matchLabels:
            app: fastapi
    template:
        metadata:
            labels:
                app: fastapi
        spec:
            serviceAccountName: fastapi-sa
            containers:
                - name: fastapi
<<<<<<< HEAD
                  # 깃허브 액션이 배포할 때 자동으로 최신 이미지 태그로 바꿔치기 합니다.
                  # (지금은 latest로 두셔도 상관없습니다)
                  image: de31/minecraft-fastapi:0d81bf7631e864bf9e977bdee7be12f5ac07e502
=======
                  # TODO: 아래 이미지 경로는 실제 빌드하고 푸시한 이미지 경로로 변경해야 합니다.
                  # 예: your-dockerhub-username/minecraft-fastapi:v2
                  image: de31/minecraft-fastapi:0d81bf7631e864bf9e977bdee7be12f5ac07e502
>>>>>>> 7d138fe5d29e96ce09aa6bea8ddbf18de28a1fdd
                  imagePullPolicy: Always
                  ports:
                      - containerPort: 8000
                  env:
                      - name: K8S_NAMESPACE
                        value: "minecraft-servers"
                      - name: GAME_DOMAIN
                        value: "mc.msdca.shop"
                      - name: API_DOMAIN
                        value: "mc.msdca.shop"
                      - name: NFS_SERVER
                        value: "100.75.219.111"
                      - name: NFS_BASE_PATH
                        value: "/mnt/nfs-minecraft"

                      # 기존 API 키 설정
                      - name: INTERNAL_API_KEY
                        valueFrom:
                            secretKeyRef:
                                name: fastapi-secrets
                                key: api-key

                      # 👇 [추가된 부분] 파이썬 에러 해결을 위한 벨로시티 시크릿 연결!
                      - name: VELOCITY_SECRET
                        valueFrom:
                            secretKeyRef:
                                name: fastapi-secrets # 쿠버네티스 시크릿 이름
                                key: VELOCITY_FORWARDING_SECRET # 시크릿 안에 들어있는 열쇠 이름

                  resources:
                      requests:
                          cpu: 500m
                          memory: 512Mi
                      limits:
                          cpu: 2
                          memory: 2Gi
                  livenessProbe:
                      httpGet:
                          path: /health
                          port: 8000
                      initialDelaySeconds: 30
                      periodSeconds: 10
                  readinessProbe:
                      httpGet:
                          path: /health
                          port: 8000
                      initialDelaySeconds: 10
                      periodSeconds: 5
